name: Extension + Raise Prompt Generation Check

on:
  push:
    branches:
      - work/prompt-generation-extension-raise-20260925
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test -- src/generation/parse.test.ts src/generation/generate.test.ts
      - run: npm run build
      - run: npm test
