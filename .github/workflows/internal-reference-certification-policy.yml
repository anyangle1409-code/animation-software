name: Internal Reference Certification Policy Check

on:
  push:
    branches:
      - work/internal-reference-certification-policy-20260925
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test -- src/reference/certification.test.ts src/reference/evaluate.test.ts
      - run: npm run build
      - run: npm test
