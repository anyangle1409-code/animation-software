# Minimal-usage validation prompt — R2 reference QA

> Open branch `work/internal-reference-engine-r2-scaffold-20260925`.
>
> Read only `docs/INTERNAL_REFERENCE_R2_SCAFFOLD_STATUS.md`.
> Do not redesign anything and do not merge.
>
> Run `npm run typecheck`, the focused `src/reference/*.test.ts` tests listed
> in that document, and `npm run build`.
>
> If green, run `npm test`.
>
> Report only READY/NOT READY, exact HEAD, failing file/test/type error if any,
> and full-suite result.
>
> Do not change exercises, rig, grip, assets, reference thresholds or family
> builders to force a pass. If a draft reference expectation fails, report the
> measured value instead.
>
> If all checks are green, the next code task is only the thin local
> `ReviewCaptureBridge.tsx` adapter into the existing R3F viewport.

