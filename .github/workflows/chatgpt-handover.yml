name: ChatGPT handover
on:
  push:
    branches:
      - chatgpt/absolute-retarget-imports
permissions:
  contents: write
jobs:
  handover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update shared changelog
        run: python .github/chatgpt_handover.py
      - name: Commit handover
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add AI_CHANGELOG.md
          git commit -m "docs: hand off muscle biomechanics upgrade"
          git push
