# V15 deep-hand session — 2026-09-25

## Decision

No V15 attempt is accepted as the hand geometry baseline. Keep V8 as the accepted body/knee geometry and V13e as the valid hand source. Do not begin the grip refit or change production references. The hand-anatomy visual/fold gate is a genuine stop condition after multiple candidate-side approaches.

## Environment and frozen state

- Mesh branch started at `cd687edbb9a461fc31d1fd841414c9a8c231d2a9` (`work/v15-deep-hand-rebuild-prep-20260925`).
- Published attempt checkpoint: `f54c331bb43eb7d46052e10e024f72e31065d12a`; Windows validation helper follow-up: `a4cbcc3`. Run `git rev-parse HEAD` for the final branch tip after subsequent report commits.
- Blender 5.2.1 LTS ran successfully on Windows.
- V8 and V13e binary/Blend baseline hashes passed `START_V15_HAND.bat` preflight.
- Frozen comparison runtime: `614033b256d869230ea273522620467401b0bc71`; frozen hierarchy: `hgpt_canonical_v3`, 63 bones.
- Candidate edits stayed in `HIGH_DETAIL_MESH_WORK`. No production asset, production reference, rig, exercise, grip, equipment, retarget or `bundled.ts` change was made.

## Attempts preserved

| Attempt | Source | Geometry/topology | Result |
| --- | --- | --- | --- |
| V15a | V13e | Up to 3.0 mm radial shaft reconstruction; 560 internal edge redirects | Rejected. Finger pinching in curl close-up; Blender digit folds over 100° rose from 3 to 95. Dressed and bare GLBs exist but must not be promoted. |
| V15b | V13e | Up to 1.2 mm controlled shaft reconstruction; 3 safe edge redirects | Best completed comparison trial, **not accepted**. Frozen guards and matched rendering pass. Bind seam audit folds over 100° rise from 4 to 8; broad shaft segmentation persists in open/fist views. Dressed and bare GLBs exist. |
| V15c | V13e | Joint-loop trial | Stopped before export: topology script misidentified original vertices as new and reset stable tracking IDs. Keep the Blend for diagnosis only; never export or promote it. |
| V15d | V13e | Corrected tracking, one-digit joint-loop trial | Diagnostic Blend only. Topology operation invalidated stored BMesh vertex handles for later digits, so it did not process both hands symmetrically. |
| V15e | V13e | Up to 1.8 mm shaft reconstruction and 1,165 added local PIP/DIP control vertices across eight digits | Blender invariant audit passes exact original IDs/contacts and non-digit scope, but >100° digit folds rise from 3 to 7 and sharp-edge ratios worsen. Keep Blend only; no GLB export or promotion. |

The V15a and V15b modelling scripts and their JSON reports are in `scripts/sculpt_v15*_digit_cores.py` and `reports/sculpt_v15*_digit_cores.json`. The V15c–e Blends are useful negative controls, not approved candidates.

The pre-edit V15a checkpoint is `checkpoints/v15_manual/v15a_deep_hand_rebuild_checkpoint_001.blend`; its hash and byte count are in `V15_ATTEMPT_MANIFEST_2026-09-25.json`.

## V15b measured validation

- Original V8 source IDs present; 682 protected push-up vertex positions and skin rows unchanged; non-digit positions and weights unchanged.
- Original digit movement maximum: 1.2001 mm. Blender degenerate, nonmanifold and digit-boundary counts do not increase.
- Bare/dressed posed body difference: 0.0000 mm. Bone and equipment matrix differences: zero in the seven sampled exercises. Push-up floor difference: zero.
- Frozen `614033b` guards: 5/5 focused tests pass; hand exercise comparison passes. The matched visual-change calibration exceeds the rejected V14e *magnitude* but does not certify anatomy.
- Hand seam audit: V13e 4 bind folds over 100°; V15b 8. No new fingertip holes or open seams. This worsening prevents acceptance.
- Direct V13e comparison boards: `renders_v15b_deep_hand_rebuild/V15B_V13E_OPEN_HAND_COMPARISON.jpg`, `V15B_V13E_CLOSED_FIST_COMPARISON.jpg`, and `V15B_V13E_EXERCISE_HAND_COMPARISON.jpg`.

The first V15b review pipeline invocation stopped because it assumed V13e open/fist pose files already existed. These were produced using the existing frozen-runtime `v11_hand_studies.test.mts` for V13e and V15b, then `finish_v15_hand.py` completed successfully. The failure was review setup, not candidate motion or contacts.

## Current-source integration

`run_v15_latest_source_validation.py` required Windows/sparse-checkout fixes in this mesh candidate branch: use `npm.cmd`/`npx.cmd`, disable sparse checkout only inside its disposable validation worktree, and make console output safe for Unicode Vitest symbols. No source work was merged. The source commit fetched was `47187360b5d631d438a6b33b284ad06732e244cb`.

The first full-suite attempt ran after the checkout fix but could not record its output because the Windows cp1252 console rejected a Vitest check mark. Its exit code and individual failures are therefore **not verified**. A diagnostic rerun with the full suite skipped completed the current-source **production** focused gate set: 9 files, 95/95 tests passed. The V8 run was still computationally active after several minutes. It was stopped because V15b had already failed the separate anatomical fold/visual acceptance gate, so extended V8/V13e/V15b current-source comparison would not make it eligible for acceptance. The disposable source worktree was removed. Current-source integration for V15b remains incomplete; do not report it as passing.

## Next safe action

Start another uniquely named V13e-derived Blender candidate only if the ring/pinky shaft/joint patches can be rebuilt while lowering severe folds and improving the matched open/fist silhouette. Use the prepared `V15_*_CORE` and `*_ANCHOR` groups, retain every original stable ID and all 682 protected contacts, and check topology/folds before export. An additional subdivision or radial smoothing pass alone has already proven insufficient. Keep grip Phase C on hold and leave production untouched.

The exact new-attempt command, when a modeller can perform that local patch reconstruction, is `START_V15_HAND.bat v15f_deep_hand_rebuild` from `HIGH_DETAIL_MESH_WORK`; run the Blender invariant/fold audit before `RUN_V15_POST_EDIT_ALL.bat v15f_deep_hand_rebuild`. The last completed full frozen/matched command for a review GLB was `python scripts/finish_v15_hand.py --version v15b_deep_hand_rebuild` (passed). No subjective choice between two acceptable candidates is pending: all attempts fail the anatomical gate. Meaningful progress now requires a different local topology approach with direct anatomical inspection, not a threshold or rig change.
